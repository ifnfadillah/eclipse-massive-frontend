const Judul = (props) => {
  const { children } = props;
  return <h2 className="mb-4 text-4xl font-primary tracking-wide font-medium text-neutral-800 lg:text-4xl sm:text-3xl">{children}</h2>;
};

export default Judul;
