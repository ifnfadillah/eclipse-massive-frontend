const Judul = (props) => {
  const { children } = props;
  return <h2 className="mb-4 text-3xl md:text-4xl font-primary tracking-wide font-medium text-neutral-800">{children}</h2>;
};

export default Judul;
